## 📌 Project Overview

This project demonstrates an end-to-end data cleaning, transformation, and visualization workflow using a **synthetic retail supermarket dataset** created for learning and portfolio purposes.

The project focuses on converting raw, inconsistent retail data into a clean and structured dataset using Python. It includes handling missing values, removing duplicates, transforming data, and creating visualizations to generate meaningful business insights.
